# cat_util
Implementation of the cat utility in C
## Logic
```
CAT - concatebale and print files
├───int main(int argc, char **argv) { return 0;}
└── 📁Базовая функция, способная принимать аргументы командной строки: int main(int argc, char **argv) { return 0;}
    ├───Функция, которая открывает и выводит файл open_print_file()
        ├───Функция открывает текстовый файл для чтения FILE *filepoint = fopen(имя_файла, режим_открытия); (определена в stdio.h) # https://metanit.com/c/tutorial/7.1.php
            ├─── первый аргумент - имя файла,
            ├─── второй аргумент - режим файла, или что мы хотим с ним сделать ("r": текстовый файл открывается для чтения)
        ├─── условие, которое выполняется, если функция fopen() != NULL
            ├─── переменная принимает символ из потока filepoint -> int sign = fgetc (filepoint);  # https://ru.manpages.org/fgetc/3 
            ├─── запускаем цикл посимвольного чтения и посимвольного вывода пока переменная sign не приняла EOF
                ├─── выведем полученный символ putchar(sign);
                ├─── присвоем переменной новый символ из потока sign = fgets (filepoint);
            ├─── после завершения цикла (получения EOF) закрываем файл fclose(filepoint);
            ├─── обнуляем указатель на файл filepoint = NULL;
    ├───Вывод нескольких файлов - через цикл который использует функцию открытия и вывода файлов
        ├─── для каждого из аргументов строки начиная с argv[1] : for (i=1; i<argc; i++)
        ├─── вызываем функциию открытия и вывода файла open_print_file()
    ├───Работа с флагами
        ├───Парсинг командной строки - в продолжении
```
